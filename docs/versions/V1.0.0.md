# V1.0.0

发布日期：2026-09-06。

## 发布内容

- 完整 P0–P4 ETF 研究工作站源码包：Vue 工作台、搜索与统一 ETF 详情、KLineCharts 图表、持仓导入/OCR 候选流、研究任务/审核/Bridge、因子诊断与组合风险基础。
- 保留 FastAPI、Alembic、Provider Adapter、认证、确定性指标和唯一 current action；不连接券商、不自动下单。
- 本地演示绑定回环地址，使用 Mock 与临时 SQLite，明确 `actionable=false`。

## 数据源与资格

- Mock：用于本地演示与测试，不得标作实时或操作级信号。
- Tushare、AKShare、FTShare 与 composite：代码适配与隔离测试纳入本版本；真实凭据、数据商权限、行情时效、字段口径和生产网络资格仍需独立验收。
- 14:30 历史 PIT、样本外校准、成交约束和 Shadow Run 仍为 `not_qualified`。

### 本次本机只读核对

- Provider/Composite/AKShare/FTShare/市场上下文的隔离回归通过；这些验证适配、回退、字段校验和资格门禁，不等同线上数据商持续可用。
- Mock：3 个样本的结构资格检查通过，`actionable_count=0`，符合演示数据不可操作的合同。
- AKShare：目录样本 5 条可用；`510300.SH`、`512480.SH` 各取得 263 根日线，OHLC、日期和重复检查通过；现价在 20 秒预算内超时。当天交易日历判定为非交易日，因此本次没有可操作级现价。新闻取得 155 条，仅证明接口此次可读。
- FTShare：列表、日线、现价三项有界探测均被拒绝，保持 `unqualified`，没有加入运行时回退链。
- Tushare：本机未配置 Token，未进行真实调用；RSS：未配置来源，未进行真实调用。

## 验证与回滚

- 原始 ZIP 清单校验：517 个列出文件、0 mismatch。
- 本机发布复测：后端 `pytest -q` 退出 0（784 条已收集；Windows 平台条件项跳过）；Provider/Composite/AKShare/FTShare/市场上下文专题回归退出 0；Vue 19 项测试、类型检查和生产构建通过；Alembic SQLite 升级/check 通过，head 为 `d40609090002`。
- 隔离 demo API/UI smoke 的业务断言通过；Windows 退出清理临时 SQLite 时仍可能出现 `WinError 32`，不影响运行中的回环 demo，但应在后续脚本维护中修复。
- 回滚：切回上一个 Git 标签/部署产物；不删除真实数据库或持仓数据。
- Windows 回归兼容：外部研究包测试按字节写入保留原始换行；没有符号链接权限的 Windows 环境跳过该项安全演练，其它平台继续执行真实 symlink 拒绝检查。
- 因子 Spearman 统计将 SciPy 声明为运行依赖，避免安装完整包后因缺少可选库而无法执行相关矩阵。

## 兼容性

- 数据库迁移头由 Alembic 管理；生产升级前先备份并演练迁移。
- 本地无认证 demo 仅供界面验收，不得公开暴露。
- `DELIVERY_MANIFEST.json` 仍保留为原始 ZIP 的只读校验清单；本版本在原包基础上新增版本记录、依赖与平台兼容修复后，该原始清单不再用于校验发布工作树。
