# 2026-09-13 审核修复接续记录

依据用户提供的 `CODE_AUDIT_BLOCKERS_20260912.md`，不改写原审核结论。接续远端已存在的审计分支 `fix/v105-audit-blockers-20260912`，首次核对应用头 `e311a3864a909699c8e3da553529878b2ecc1ef6`。该树包含此前逐标的历史资格、缺失值掩码、全历史哈希、交易日门禁和跨进程流水线锁的部分修复；本轮不会把它们算成新编写代码。原 main 和生产未修改。

## 第一阶段：资格契约补漏

- 复跑既有审核数据/任务/接续专项32项通过。
- 新增未知复权基准、日期缺失、字符串数字OHLC与量额的6项拒绝/接纳样本；先失败，修复后与既有数据专项共34项通过（隔离Python3.13.5，无真实行情）。
- 新浪v101/v102依然不能凭均价比值获得绝对单位资格。旧记录不乘系数、不删除；异常只阻断相关研究，不把价格展示等同可操作。
- 未知复权字符串、非日日期、错误OHLC拒绝；合法可转换的数值必须用数值比较，不使用字符串字典序。

## 588200 外部证据（不是对生产行的认证）

本轮查到基金管理人正式信息披露：2026-07-15公告列明588200权益登记日2026-07-20、除权日2026-07-21，1份拆为3份。
来源：https://app.cnstock.com/zzb/zgzqb/html/2026-07/15/nw.D110000zgzqb_20260715_1-B003.htm
复核日期：2026-09-13。只保存事实摘要与来源，未复制整篇公告。

这补充了原审核尚未取得的公司行动证据；并不证明库内3.539和1.349或整段volume/amount正确。原始序列保留，须独立同日行情及拆分结果对账，再生成独立复权收益序列并记录因子与输入哈希。不能直接修改旧价格或让一个公告URL绕过数据资格。当前大断点门禁仍保留；35%只是保守异常触发阈值，不证明所有较小断点都没有公司行动。

## 原CI失败核验

已读取实际失败日志：workspace-ci运行34612255965，job103305386997，`backend/tests/test_v105_handoff.py:111`的`from backend.tests.test_v103_history import instrument`报`ModuleNotFoundError: No module named backend`。后续构建/浏览器步骤因此跳过，不是Node弃用警告造成失败。此导入在接续树中已修正；仍需最终固定提交的完整CI重新验证。

## 本轮后续阶段

1. 按阶段目标交易日独立恢复指标/预测任务，不能以日线成功代替衍生任务成功；非有限计数、输入数量不作完成证据。
2. Bridge官方登录仅设置子进程HOME/CODEX_HOME，Windows检查ACL；非法结果明确失败，有效结果传输失败只重传；本地一次进程批准绑定job/input/model及时间，不宣称CLI提供费用封顶。
3. 旧壳等待按任务ID终态收敛；发布收据绑定源码、构建、迁移和镜像证据。
4. 记录真实Windows/数据权限/本人模型登录及CI审批的未执行项。本文不是部署成功声明。

### 实测发现的计算性能回归

初次全量停在校准验证；保留日志并用faulthandler定位到pandas `__finalize__`反复deepcopy每行每因子的attrs布尔掩码。掩码已体现为输出NaN，因此删除重复的大型attrs网格，只保留策略标识，不改变数值或恢复缺失值。新增结构大小和NaN保持测试。前两次长跑是中止诊断，不计为通过；最终全量需另有退出码。
