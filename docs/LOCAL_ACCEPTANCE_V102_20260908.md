# 原版 ETF 模板回归总览：本地接收与验收

日期：2026-09-08。实施入口：PR #30。目标分支：`fix/v102-home-template-20260908`。

## 1. 基线与提交边界

用户确认的基线是 `127ee3d0c8bb572af87c4e1cf906fec564a74e63`，位于 `codex/v1.0.1-data-access`。不得用仍停留旧版的 main 替代这条基线，也不得覆盖另一条 `codex/v1.0.1` 的并行改动。

本次已核对的应用修复提交为 `1bafeac04b8e1613e987c7aa165b1dedb3eaf605`。该提交之后的文档补充不是生产已部署证明。不移动已有版本标签，不自动合并 main，不自动部署生产。

本文件属于代码接收指令，不要求本地 Agent 重新设计或重写前端。

## 2. 必须保留的界面合同

1. 未登录展示独立的登录界面，提供密码显隐和清楚的错误状态。邀请码注册遵循后端开关，注册不直接授予管理员权限；关闭注册时解释原因。已有管理员可通过账户管理流程创建成员。
2. 登录后市场总览是固定主页。左侧导航、顶部搜索与底部账户区属于同一工作站。ETF 决策快照必须放在总览中的对应位置。
3. 决策快照使用原 `decision_board_workbuddy.html`、原 CSS 与指标表格渲染器；不是将原表简化后另挂一个“经典版网站”。同源嵌入只为隔离旧 CSS/DOM，不增加第二套登录、导航或决策计算。
4. 保留五档分组、原高密度字段、排序、筛选与 1/3/5/10 日切换。点击基金由宿主工作站进入 `/etf/{ts_code}`，返回保留筛选与预测期限。验证浏览器返回和页面提供的返回入口。
5. 旧 `/matrix`、`/classic/etf-board` 只作为兼容入口，归一到总览的决策快照位置。不能再次将它们设置成独立登录后主页。
6. 行业与概念是两个不同的数据分类，均复用既有 `SectorSnapshot`、`MarketService` 与 Provider。搜索使用已同步证券目录，包含未进入跟踪池的 ETF/LOF，不只搜行业 ETF。目录总数不等于经过交易所全量对账的发行总数。
7. 目录同步不删除持仓、自选，不自动将全市场加入昂贵的指标/预测计算池。GET 搜索和页面导航不抓行情、不运行模型。

## 3. 远端证据索引

以下链接是特定提交的执行记录，不应将较早成功记录套用到后续代码：

- 应用提交：https://github.com/Jovifei/ETF-Fund-Analysis/commit/1bafeac04b8e1613e987c7aa165b1dedb3eaf605
- workspace-ci（含真实 Chromium 与真实账户会话流程）：https://github.com/Jovifei/ETF-Fund-Analysis/actions/runs/34177334209
- 完整 ci：https://github.com/Jovifei/ETF-Fund-Analysis/actions/runs/34177334221
- 公开数据源观察：https://github.com/Jovifei/ETF-Fund-Analysis/actions/runs/34177331879

workspace-ci 的真实浏览器流程使用隔离数据库和明确的测试行情，不证明实时市场数据已合格。公开数据源观察必须阅读产物内每个能力的返回数、失败原因和源时间；工作流退出成功本身不等于接口成功。Tushare Token 不在仓库，不复制任何人现有认证文件。

## 4. 本地 Codex 执行 Prompt

你执行本仓库修复代码的接收、复测与本地持久部署，不重新制作界面，不合并 main，不部署生产。

### 接收代码

- 只读检查当前工程路径、remote、HEAD、分支和脏文件名，不显示私人文件内容。
- 不 reset/clean/stash，不覆盖未提交改动。使用独立 clone 或新 worktree。
- 拉取 `fix/v102-home-template-20260908`，先固定到 `1bafeac04b8e1613e987c7aa165b1dedb3eaf605` 进行应用验收。后续提交仅在审查 diff 后接收，不能按分支名默认信任新变动。
- 确认 `127ee3d0c8bb572af87c4e1cf906fec564a74e63` 是目标提交祖先。
- 阅读 `AGENTS.md`、`STATUS.md`、`HANDOFF.md`、`docs/README.md`、本次审计文档以及现有私有数据部署记录。
- 不从旧完整 ZIP 再次覆盖此分支，不从旧 main 重做。

### 安装与测试

- 使用独立 Python 3.12 环境和满足 engines 的 Node，按 pyproject 的 dev/market 依赖及 frontend 锁文件安装。
- 执行后端 pytest 全量、Python compileall、前端 npm ci/test/typecheck/build、旧 JavaScript 单测、git diff --check、密钥扫描。
- 按 `.github/workflows/workspace-ci.yml` 的实际命令，执行普通 Playwright 与独立认证浏览器测试。不能只运行组件单测后声称浏览器通过。
- 测试数据库必须独立，不得指向现有用户数据库。PostgreSQL 条件测试如跳过，记录理由。
- 检查 Alembic 单一 head；先备份真实库，在副本演练后才对用户批准的本地持久库迁移。已有数据库不得重新 init。
- 失败保留日志、定位最小问题，不删除测试、不放宽数据真实性或认证规则。

### 本地持久服务

- 找到并沿用用户已有的本地私人配置、数据库、报告与账户；不打印 Token、密码、Cookie 或持仓内容，不提交 .env、数据库或备份。
- 应用源码置于新工作树，运行数据与凭据保留仓库外。
- 使用 `scripts/run_workspace_live.py --help` 和仓库部署文档确认实际 CLI；不要把临时 Mock demo 当作真实部署。启动 API 与配套 worker，均绑定本机，使用同一数据库与同一代码版本。
- 已有管理员沿用原账户。空库才运行既有安全初始化流程，密码由用户在本地隐藏输入；不建立写死密码或从公开聊天复制密码。
- 若需要注册普通用户，在本地私有配置明确启用注册并设置邀请码；无需注册时保持关闭，仍要验证管理员创建成员可用。不要为本地注册修改生产注册策略。
- 真实模式禁用 Mock 回退。没有 Tushare Token 时明确使用可用公开源并记录未配置状态，不宣称 Tushare 已接通。

### 行业、概念与全目录验收

- 启动后通过已有任务接口/页面实际执行证券目录同步和板块同步，观察排队、worker 领取、成功/部分成功/失败、持久化和页面刷新。
- 对行业与概念分别报告条数、数据日期、来源及同步失败信息；涨跌为 0 必须显示 0，不得变成缺失。
- 搜索至少覆盖宽基、行业、债券、货币、跨境、商品 ETF 和 LOF；选择一个未在原跟踪池且名字不含 ETF 的目录标的验证。确认分页不重复不遗漏，证券代码前导零保持。
- 搜索四动作分别验收：查看详情不写入；加自选去重；持仓录入预填且确认后写入；AI 只打开草稿不调用模型。
- 自动发现默认关闭；完成手动同步与资源核对后，需要定时才显式启用代码定义的低频发现设置。只保留一个任务权威，避免旧 scheduler 和新 worker 重复采集。
- 核心来源失败应修复本机配置/依赖/网络问题或给出具体阻塞，不能把空白页面当接入成功。没有有效行情的 ETF 能被搜索，但图表必须诚实显示未初始化，而不是用别的基金或 Mock 替代。

### 用户可见验收

在实际本地 HTTP 地址逐项操作并截图：

1. 未登录只有登录/注册相关界面，不显示决策表作为登录页。
2. 正常登录直接进入市场总览，完整原版 ETF 表在总览的“ETF 决策快照”位置，只有一套工作站导航。
3. 行业、概念分类都可查看；新旧表与现有服务读取的是同一份数据，不制造第二份动作。
4. 快速切换期限并输入筛选，不被迟到请求或 frame 消息清掉；点击 ETF 后返回，筛选/期限仍在。
5. 旧看板书签归一到总览；刷新、前进、返回没有循环或跳到另一个主页。
6. 错误密码、密码显隐、注册开关/邀请码、管理员创建成员、普通成员隔离、退出后清理旧状态全部检查。
7. 数据与账户在应用重启后仍存在。记录应用代码 SHA、运行路径、端口和进程，避免实际上还在访问旧目录的服务。

### 交付与停止条件

交付实际本地地址、固定代码 SHA、测试结果、原模板总览截图、行业/概念同步统计、全目录搜索示例、账户流程结果、私人备份位置和未完成边界。详细日志中的凭据和用户内容脱敏。

本任务不授权生产更新、标签移动或 main 合并。完成本地验收后停止，等用户确认页面和真实数据展示，再另行处理服务器部署。不把改前端、指标算法或研究策略重新作为本地接收任务。
