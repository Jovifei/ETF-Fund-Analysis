# 全文件交付过程记录

## 执行来源

用户改为“完成后给全文件压缩包，由自己提交”。本轮只读远端，不新建/改动PR，不部署用户电脑/ECS。
读取main及PR28，核对恢复的tree等于远端7adf树，之后在隔离目录开发。Git本地历史只用于核对，不随包分发。

## 实际工作记录

1. 恢复开发源码与固定Node依赖，检查AGENTS、状态、实现文件和测试，纠正历史对话里误称没有代码的说法。
2. 建立Python隔离测试环境；记录缺失Docker/PostgreSQL/实时Provider等实际环境限制。
3. 基线全量pytest，定位模拟AKShare测试隐式安装依赖，改显式注入而不删测试。
4. 写行情批量写入失败测试，复现100bar/103SELECT；实现有界批量读取、校验、修订和幂等写入。
5. 复现会话切换迟到请求，添加APIepoch/session序号；补服务端偏好、移动焦点和清缓存。
6. 实现原生Vibe选定产物导出→预览→同意→未验证私有候选→人工审核，补身份/时间/hash/重复/用户隔离测试。
7. 新增版本化风格模板；修正worker部分成功语义和非法报价估值；保留原算法和研究资格。
8. 修复图表遮挡标签，补MA30、行情色、成本线和当前日线快照时点说明。
9. 全量pytest782通过1跳过；相关后端33通过；Vue19通过/type/build通过；旧JS19通过；SQLite迁移与ASGI页面验收。
10. 标准HTTP浏览器导航被管理策略阻挡；未改策略。另建实际组件+真实ASGI离线浏览器harness，5组流程、0页面错误、28API调用通过；截图明确Mock。
11. 移除一次性自动改代码/提交CI；Vibe上游安装CI改为手动；核心CI保留完整门禁。
12. 主站UI/部署/研究包/自提交/验收文档收敛；保留旧正文与上轮规划供离线追溯（归档仅规范化行尾空白，旧相对链接按原目录理解）；生成全源码+前端产物和SHA256清单。

## 未做且不能写成成功

没有真实模型调用，没有真实上游完整报告，没有正式中文OCR引擎安装，没有生产数据采集/写库/部署，没有生产PostgreSQL/Docker/HTTPS端到端验收，没有盈利有效性封版。
目前因子注册与模板不晋升策略；historical_1430_backtest仍not_qualified。

## 后续接手

先读 START_HERE → DELIVERY_P0_P4 → TEST_REPORT → LOCAL_WORKSPACE_DEPLOYMENT，再看planning-v2。
后续可继续改代码，但应先保护用户工作树并复跑测试，不重做信息架构，不把历史规划中的可选重量级系统全装进API容器。
