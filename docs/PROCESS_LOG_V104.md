# v1.0.4 过程与证据

基线复核2026-09-10：远端main为57470ea，不再是旧3c7bdc7；包含本地后续修复。独立功能分支，不覆盖并行代码。

1. 目录问题重现：行业只字面搜索、分类返回过少仍停止后备。专项测试先加入元器件关联、小目录后备、不扩计算池、GET不抓取、worker状态。应用提交55c95b8c。
2. 图表与研究：自然周/月聚合、价格辅助线组与阈值区分、共享1/5/20展望、原模板排序星标、同步TaskProgress。应用提交8e1bd483；型号/策略旧合同不改。
3. AI/新闻/账户：增设个人加密profile及单次任务、限额/过期/撤销/归属/错误脱敏测试，会员管理、新闻时间和帮助，版本1.0.4。未通过本人模型真实调用，不将隔离测试记作现场验收。
4. 全量本机回归曾出现两项旧合同失败：要求1只ETF小响应标fallback_used，以及要求原表仍含用户明确移除的较昨日列。修订为小样本partial检查和重复列不存在/保留其他列/可排序断言；不删除测试。另修复未取到小时数据后周期按钮一同被隐藏的问题。
5. GitHub写入曾被大段传输截断，预设hash门禁拒绝应用；改分块校验并通过测试后生成实际源码提交。临时传输文件/应用工作流必须在最终树清除。传输提交不是应用完成证据。

最终测试结果、固定SHA与本地Prompt在PR收据追加；本文件中“已实现”不等于真实数据权限或部署成功。保留原STATUS/HANDOFF/知识库，不重新压缩历史证据。
