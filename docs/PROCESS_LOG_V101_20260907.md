# v1.0.1 开发过程记录

## 授权与基线

用户要求审核本地 Codex v1.0.0 提交/部署，优先完成后端数据接入并保留原 ETF 面板；前端外壳已通过，避免推倒重做。此前允许通过全文件 ZIP 自行提交。
核对分支/标签指向 9a0ca181，main 仍 3c7bdc7；独立 GitHub Release 查询 404。读取官方上游接口文档，下载成功 CI artifact 9991888936 并固定源码基线。
本段是部署前过程快照：当时只在 GitHub 预建 feat/v1.0.1-data-access，起点 9a0ca181；应用修改在隔离工作区完成，交付 ZIP/patch，尚未推送源代码或进入用户机器/生产。最终接收、推送与部署见文末“接收后部署”。

## 实现顺序

1. 先复现参数、单位、时间与任务耦合缺陷，新增失败测试。
2. 固定 HTTPS/子进程硬截止/有界响应；Tushare 沪市实时与源时间、日线单位、30/60 分钟；AKShare 价格回退、时效与 ETF-only 请求；RSS 独立组合。
3. 来源口径版本化，整段旧历史覆盖才能原子替换。价格回退缺量不补零，不让旧量/新量混用；恢复量能时重抓最早缺量历史。修订写入新的 fetched_at，不冒充过去可见。
4. 分离行情、现价、目录、新闻、板块、分钟任务；旧任务错误/部分缺失不显示绿灯；任务心跳遇 SQLite 锁继续监督，退出清理自建进程，避免孤儿任务。
5. 接入状态端点与现有 settings 集成；不读出凭据值。保留原版 /classic/etf-board，增加 Vue /matrix，点击只去统一 /etf/。
6. 持久认证启动与原 Mock demo 分开，明确回环监听、外部私有配置、无自动数据初始化。修复 demo 的退出资源清理；保留 Windows 待实测边界。
7. 全量/局部/编译/构建/迁移/只读 HTTP/离线浏览器复验。最后全量 812 项、811 通过、1 跳过、0 失败。更改旧断言有安全收紧依据，记录在 VALIDATION_V101.md。
8. 更新 docs/versions，每版内容、资格、验证、回滚和未完成边界分开；归档旧根交接/启动/提交文档。制作精确基线补丁和完整包，清单校验不包含密钥与用户数据。

## 未取得的证据

没有用户 Windows 实时进程/本机网络数据、Tushare 账户权限、FTShare 可用资格、真实 OCR/模型、生产 Docker/PostgreSQL/TLS、本版云端 CI、连续交易日影子运行。不能用 APP_VERSION、Mock、截图或上一版 CI 填充这些项。

## 接收与复验记录（2026-09-07）

接收 ZIP 的 SHA256 与预期一致；原工程 `E:\project\ETF-Fund-Analysis` 保持原脏区未改。新 clone 从 `9a0ca181` 建立 `codex/v1.0.1-data-access`，未重置远端已有 `codex/v1.0.1`。

本机完成 Python 3.12 依赖、812 项后端测试（0 失败、5 条 Windows/外部服务条件跳过）、Vue 19、标准 Playwright 5、PostgreSQL 16 条件测试、ShellCheck、Compose、Alembic、密钥扫描和 Docker 镜像 smoke。Playwright 首次失败暴露 Matrix `th` 可访问性和复跑数据库状态污染，分别以 `scope=col` 和专用 fixture 清理修复后重跑通过。

AKShare 公共探测和两标的真实任务均按数据合同收敛：Sina 日线价格可读但缺量，衍生计算阻断；没有静默转 Mock。生产 SSH 只读盘点发现旧 0.8.0 根 Compose/API 8080/旧 scheduler/较早迁移头；已完成生产数据库备份并校验，不执行未经 staging 恢复与单位核验的切换。

## 接收后部署（2026-09-07）

提交 `224b59f` 已推送，`ci` 与 `workspace-ci` 均成功。由于服务器不能从 GitHub clone（出口返回空回复），通过 SSH 传输已校验 ZIP 到临时目录并加载本机构建镜像；传输包 hash 与本地一致。远端 staging 恢复生产 SQL 备份，Alembic 从 `c2d3e4f5a6b7` 升到 `d40609090002`，OHLC、重复键和单位完整性检查通过；API/worker HTTP smoke、重启保留和私有 API 401 通过后清理全部临时资源。

生产维护窗口先停止旧 API/scheduler，再执行目标镜像迁移；发现旧 `.env` 的 `APP_VERSION` 覆盖后，在非敏感生产 Compose 覆盖中显式固定 `APP_NAME`/`APP_VERSION`，并为 worker 修正 heartbeat healthcheck。最终 API 与单 worker healthy，正式 HTTPS health 200，旧 scheduler 未重启，Nginx 未重载。Provider 探测保持未资格化：AKShare Sina 日线缺量、Tushare 能力调用未通过；因此生产使用 `public_composite`、关闭 Mock fallback/模型/OCR/分钟线/定时复盘，不生成新的操作级信号。

所有生产变更均保留可回滚点：数据库 SQL 备份、reports/配置/Nginx 归档和停止的旧容器；未读取或回显 `.env`、Token、Cookie、密码、持仓或备份内容。
